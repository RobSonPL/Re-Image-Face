
// Stripe service disabled per user request
// Placeholder logic to prevent import errors

export const initiateCheckout = async (type: 'credits' | 'subscription'): Promise<void> => {
  console.log("Payment logic is currently disabled.");
  return Promise.resolve();
};
